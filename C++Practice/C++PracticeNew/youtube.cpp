#include <iostream> //defines iostream
//note: a namespace is like a library
int main() //main function
{
    std::cout << "Hello World\n";
    return 0; //just so the application works. This line is optional, it is called implicitly anyway. Instead of zero, some main functions might instead return EXIT_SUCCESS
}